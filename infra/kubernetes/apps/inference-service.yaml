apiVersion: v1
kind: Service
metadata:
  name: fraud-inference-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: fraud-inference
  ports:
    - port: 80
      targetPort: 8000